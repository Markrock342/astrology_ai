# Never upload local secrets to Vercel — use `npm run deploy:env` instead.
.env
.env.*
!.env.example

# Local-only artifacts — never include sensitive dumps or generated previews.
backups/
output/
tmp/
