import { ScaffoldNote } from "@/components/scaffold-note";

export default function AdminPaymentsPage() {
  return (
    <ScaffoldNote title="Admin · การชำระเงิน (Manual)" owner="Both">
      สร้าง/ตรวจรายการชำระเงิน · เลือกผู้ใช้ · จำนวน/วันที่/อ้างอิง/โน้ต ·
      แนบสลิป (ถ้าเปิดใช้) · pending/approved/rejected · เปิดใช้งานแพ็กเกจ · audit.
    </ScaffoldNote>
  );
}
